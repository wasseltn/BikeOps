{% extends 'front_base.html.twig' %}

{% block body %}
<div class="container">
    <div class="col-md-3"></div>
    <div class=" col-md-6" >
    <h1>Reclamation creation</h1>

    {{ form_start(form,{'attr':{'novalidate':'novalidate'}}) }}
        {{ form_widget(form) }}

        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('reclamation_indexf') }}">Back</a>
        </li>
    </ul>
    </div>
</div>
{% endblock %}
